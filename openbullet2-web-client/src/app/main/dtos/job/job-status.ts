export enum JobStatus {
  IDLE = 'idle',
  WAITING = 'waiting',
  STARTING = 'starting',
  RUNNING = 'running',
  PAUSING = 'pausing',
  PAUSED = 'paused',
  STOPPING = 'stopping',
  RESUMING = 'resuming',
}
