export interface HitFiltersDto {
    pageNumber: number | null,
    pageSize: number | null,
    searchTerm: string | null,
    hitType: string | null,
    minDate: string | null,
    maxDate: string | null
}
