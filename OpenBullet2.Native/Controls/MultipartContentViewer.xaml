<UserControl x:Class="OpenBullet2.Native.Controls.MultipartContentViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:OpenBullet2.Native.Controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="120" d:DesignWidth="800">
    <Border
        Margin="0 0 0 10"
        Padding="5"
        BorderThickness="1"
        BorderBrush="{DynamicResource ForegroundMain}">
        <StackPanel>
            <DockPanel Margin="0 0 0 5">
                <Button Click="Delete"
                        DockPanel.Dock="Right"
                        Width="25"
                        Height="20"
                        ToolTip="Remove this content"
                        Style="{StaticResource StyledDangerButton}">
                    <StackPanel Orientation="Horizontal">
                        <iconPacks:PackIconMaterial Kind="Close" Width="10" Height="10" />
                    </StackPanel>
                </Button>

                <TextBlock
                    Text="{Binding Title}"
                    VerticalAlignment="Center" />
            </DockPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <local:StringSettingViewer
                    x:Name="nameViewer"
                    Grid.Column="0"
                    Margin="0 0 5 0" />

                <local:StringSettingViewer
                    x:Name="contentTypeViewer"
                    Grid.Column="1"
                    Margin="5 0 0 0" />
            </Grid>

            <ContentControl
                x:Name="payloadHost"
                Margin="0 5 0 0" />
        </StackPanel>
    </Border>
</UserControl>
